# Archived Research — pre-RESEARCH_PROTOCOL sketch layer

This directory holds Sean's original research sketches from 2026-04, predating the formal `.agent/RESEARCH_PROTOCOL.md` framework. The contents are **not duplicates** of `docs/research/` — they're the older planning/sketching layer that the active protocol-driven research was seeded from.

**Status**: historical record. Per `.agent/DEV_RULES.md` § "Nothing is deleted," this stays as-is.

**For active research, look at `docs/research/1_DEEP/`** — the protocol-driven layer that v4 architecture decisions were drawn from.

**For business-plan track context**, this directory IS a starting point — see `v4_0_1_IMPLEMENT.md` § *Parallel Track — Business Plan Kickoff* for how the contents here feed into a future business-plan session.

## Buckets

- `1_DEEP/` — Sean's original sketches across market positioning, collab-for-markdown, AI features, auth-and-accounts, sync-and-cross-device, monetization, native-apps, feature-research. Each carries a `1_REVIEW.md`.
- `2_FOCUS/`, `3_FINAL/` — empty. The protocol's later phases were never run against this older content; if the business-plan track needs them, fresh `1_DEEP` work in `docs/research/` is the right move, not retrofitting these phases here.
