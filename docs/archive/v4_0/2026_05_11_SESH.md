**Driving**: ship `v4_0_0_BUILD.md` then `v4_1_0_BUILD.md` → tags v4.0.0 and v4.1.0
**Type**: Build (two sequential ship cycles)

---

# 2026-05-11 — v4.0.0 + v4.1.0 ship session

## Phase A — v4.0.0

- [x] A.0 Cut `feat/v4-basics` from `dev`; stash ambient README drift (3 files, cosmetic table formatting)
- [ ] A.1 Phase 0 — DevTools spike for line-wrap below 1040px
- [ ] A.2 Phases 1–9 — parallel groups A/B/C + sequential D on editor.ts
- [x] A.3 Phase 10A — local verify (`npm run dev`, `npm run build && npm run preview`)
- [x] A.4 Phase 10B — ff-merge to `dev`, push, 🛑 PAUSE for Sean's signoff (2 follow-up fixes for line numbers)
- [ ] A.5 Phase 10C — ff-merge to `main`, tag `v4.0.0`, push tag
- [ ] A.6 Phase 10D — update `THOT_APP.md`, write `BUILD_REPORT_v4_0_0.md`

## Phase B — v4.1.0

- [ ] B.0 Cut `feat/v4-urls-anchors` from `dev`
- [ ] B.1 Phases 1–4 — `click-handlers.ts`, editor wire, theme cursor, version bump
- [ ] B.2 Phase 5A — local verify (URL/anchor cases, cursor visual, cross-browser, prod build)
- [ ] B.3 Phase 5B — ff-merge to `dev`, push, 🛑 PAUSE for Sean's signoff
- [ ] B.4 Phase 5C — ff-merge to `main`, tag `v4.1.0`, push tag
- [ ] B.5 Phase 5D — update `THOT_APP.md`, write `BUILD_REPORT_v4_1_0.md`

---

## Live log

### A.0 (done)
- Stashed 3 ambient drift files (README.md, .agent/README.md, docs/archive/research/README.md) — cosmetic table re-alignment unrelated to v4 work.
- `feat/v4-basics` cut from `dev` at 11e303c.

### A.1 — Phase 0 (deferred to local-verify confirmation with Sean)
Static analysis findings (no live browser repro yet — Claude-in-Chrome permission for localhost not active in this instance):
- `src/styles/main.css` and `src/theme.ts`: no `white-space` rules anywhere.
- `node_modules/@codemirror/view/dist/index.js`:
  - `.cm-content { white-space: pre }` (line 6624)
  - `.cm-content.cm-lineWrapping { white-space: break-spaces; word-break: break-word; overflow-wrap: anywhere; flex-shrink: 1 }` (lines 6634–6640)
- No `@media` queries anywhere in our code or CodeMirror touching `cm-*` width-conditioned rules (only `@media print` for `.cm-announced`).
- `EditorView.lineWrapping` is in our extensions array at `editor.ts:303` — CM adds the `cm-lineWrapping` class to `.cm-content`, which makes `break-spaces` win.

**Conclusion**: in the current code, soft-wrap *should* already engage at any viewport width including <1040px. The BUILD's bug description may pre-date some other fix, or the symptom may have a different root cause than a CSS override. Plan: do NOT preemptively add the `@media` `!important` rule. Instead, defer to live verification in step A.3 — if Sean repros overflow on the dev preview at <1040px, we add the override on `feat/v4-basics` before pushing.

Phase 0 is parked. Proceeding with Phases 1–9 (file-disjoint from any Phase 0 fix).

### A.2–A.3 (done)
- All Phase 1–9 edits applied per BUILD (commit 480cd8d). `tsc` clean, `npm run build` succeeds.

### A.4 — dev-preview gate + bug-fix loop
- Pushed dev `480cd8d`. Sean tested and reported issues.
- **Line-numbers fix #1** (commit 44c6529): `align-content: flex-end` was pushing wrap-line numbers to the second visual row → `flex-start`. CodeMirror default `min-width: 20px` only fit 2 digits → `25px` to hold 3 digits without 99→100 shift.
- **Line-numbers fix #2** (commit 92c1868): CM base padding `0 3px 0 5px` was consuming 8px of the 25px column with `text-align: right`, clipping the third digit and visually centering numbers → set `padding: 0`. Sean confirmed perfect.
- **Spec bugs Sean found (in dev `92c1868`)**:
  - Phase 1 (`->` autocorrect): trigger regex `[a-zA-Z0-9_:-]+` doesn't include `>`. Sean opted to **skip the fix** (revert during my attempt — agreed it's a non-sure-win). Move to v4.0.1 candidates.
  - Phase 3 (DocumentMeta remap): BUILD diagnosis was wrong — `@lezer/markdown` doesn't emit `DocumentMeta` at all. The real "hyphen below paragraph turns line orange/bold" bug is SetextHeading2 promotion. Our remap is inert. **Sean's call: leave as-is, fix in v5 with proper scope system.**
  - Phase 4 (TaskMarker `[x]` darker): doesn't visibly apply. Inline-style decoration uses the same pattern as bullet/number marks (correcting my earlier mis-read). Cause likely `tags.atom` HighlightStyle rule cascading on top. **Sean's call: leave as-is, fix in v5.**
  - Phase 7 (list bleed): Fix works for trailing-line bleed but tight lists (no blank-line separation) lose content tagging because `BulletList/ListItem/Paragraph` doesn't match tight-list inline content. **Sean's call: the bleed fix is the priority; loss of inline color is acceptable v5 work. Do not revert.**
- **Strategic decision**: All highlight-system imperfections roll forward to v5's proprietary scope rebuild. v4.0.0 is polish, not highlight-system surgery.

### A.5/A.6 — Ship + docs
- Sean confirmed Phase 6 (PWA title dedup) works.
- Sean flagged: PWA install button should NOT be offered (browser-only until native shells in vNext). Filed as v4.0.1 candidate.
- v4.0.0 = dev `92c1868`. Tagging now.

---

## Session Notes

- The original BUILD spec for Phase 3 (DocumentMeta) was diagnostically wrong — `@lezer/markdown` doesn't emit `DocumentMeta` for markdown content. The bug the BUILD was trying to fix is actually CommonMark's SetextHeading2 promotion (`---` after a paragraph). The styleTag entry we added is inert. Documented in BUILD_REPORT § Deviations.
- BUILD spec for Phase 4 said the inline-style decoration pattern would work; live test shows `[x]` doesn't visibly darken. Likely `tags.atom` HighlightStyle rule wins on cascade. Pattern is identical to bullet/number marks which DO work, so the difference is probably which HighlightStyle rule is competing. Belongs in v5.
- BUILD spec for Phase 7 underestimated CommonMark's tight-vs-loose list distinction. `BulletList/ListItem/Paragraph` only matches loose lists.
- Phase 2 (line numbers) needed two follow-up fixes that the BUILD's spec didn't cover. Worth flagging that pure-CSS-token swaps can still need behavioral testing.
- Claude-in-Chrome MCP permission for `localhost` wasn't punching through; Sean noted the docs at https://code.claude.com/docs/en/quickstart suggest a different integration path now. Future sessions may benefit from a setup pass on this.
- Two rounds of ambient drift on README/SESSION docs showed up during the branch-switch flow. Cosmetic table re-alignment; stashed both rounds. Worth investigating what process is touching them.

## Picked Up From / Stopped At

- **v4.0.0 shipped** — `main` at `92c1868`, tag `v4.0.0` pushed (after this SESH commit).
- **Next**: Phase B of the plan — `v4.1.0_BUILD.md` (URL clickability + anchor links). Cut `feat/v4-urls-anchors` from `dev` once it's at `main`'s commit (`dev` and `main` are at same commit after ff-merge).
- Working tree clean except for this SESH file and the BUILD_REPORT, both being committed in the same docs commit.
- Two stash entries remain (`stash@{0}`, `stash@{1}`) holding the ambient drift on `.agent/README.md`, project READMEs, and the older v4_0/processed/v4_0_0_REGROUP.md / v4_0_1_SESSION.md. Worth investigating their origin.

## Open Threads For Next Session

- **v4.0.1 candidates** (one or more BUGS-mode patches):
  - Add `>` to autocorrect trigger regex so `->` substitutes.
  - Remove or hide the PWA install button (browser-only product direction until vNext native shells).
- **v5 planning candidates** (highlight-system rebuild scope):
  - SetextHeading2 disambiguation vs HorizontalRule UX choice.
  - Checked-todo distinct color (cascade-safe).
  - Tight-list bleed fix that preserves content tagging.
- **Drift investigation**: what process is reformatting markdown tables and trimming sections from `docs/archive/v4_0/processed/v4_0_0_REGROUP.md` and `v4_0_1_SESSION.md`? Two rounds happened during this session. Not Claude — was already present on branch switch.
- **Orphan branch cleanup**: `feat/v4-basics` has commit `7269eb0` (the unused `->` regex fix) that didn't ship. Branch can be deleted now or kept for the regex fix if v4.0.1 wants it.
