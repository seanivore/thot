@~/.agents/AGENTS.md

# Start here

The development protocol is **global**. It lives at `~/.agents/AGENTS.md` and there is no project-local copy of it — this file is a pointer, and it is the only thing in this directory that concerns the protocol.

**Read `~/.agents/AGENTS.md` in full before doing anything.** It maps everything else: the gap-review procedure, naming and versioning, git and deploy discipline, the design method, and the reference guides.

If you are looking for `DEV_RULES.md`, it is retired. `~/.agents/AGENTS.md` replaced it on 2026-07-28.

Project-specific context is not here either — it is in `assets/docs/PROJECT_NAME.md` (architecture), `assets/docs/GOALS.md` (what this project is for), and `README.md`.
